{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Placeholder only — supplied per environment via the standard config
    // layering: appsettings.Development.json (local default) or the
    // ConnectionStrings__LoanDb environment variable (deploy), later Vault.
    "LoanDb": ""
  }
}
